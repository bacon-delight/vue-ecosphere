# Primitive tokens

Raw values: hex colors, px sizes, radii. Prefer overriding [semantic tokens](/tokens/semantic) instead — primitives are intentionally low-level.

<TokensTable group="primitive" />
